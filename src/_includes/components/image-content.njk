<section class="flex justify-between max-w-screen-xl py-8 mx-auto prose sm:py-20">
    <div class="w-full px-8 -mx-8 sm:w-2/3">
        <h2 class="mt-0 text-3xl">{{ twoCol.title }}</h2>
        {{ twoCol.content | safe}}
        <div class="flex flex-wrap gap-4 mt-8 text-center">
            <a class="block w-full px-12 py-3 text-sm font-bold tracking-wide text-white no-underline uppercase transition rounded shadow bg-sky-700 sm:w-auto active:bg-rose-500 hover:bg-rose-700 focus:outline-none focus:ring hover:shadow-xl" href="{{ hero.button01.link }}">
                {{ hero.button01.text }}
            </a>

            <a class="block w-full px-12 py-3 text-sm font-bold tracking-wide no-underline uppercase transition rounded shadow bg-sky-900 text-sky-100 sm:w-auto hover:bg-rose-700 active:text-rose-500 focus:outline-none focus:ring hover:shadow-xl" href="{{ hero.button02.link }}">
                {{ hero.button02.text }}
            </a>
        </div>
    </div>
    <div class="w-full px-8 -mx-8 sm:w-1/3">
        <figure class="mt-0">
            <img laoding="lazy" src="{{ twoCol.image.path }}"
         alt="{{ twoCol.image.alt }}">
            <figcaption>{{ twoCol.image.alt }}</figcaption>
        </figure>
    </div>
</section>